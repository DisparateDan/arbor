{
  "name": "arbor-family-tree",
  "version": "0.1.0",
  "description": "Obsidian plugin for visualising family trees from person notes",
  "main": "main.js",
  "scripts": {
    "build:html": "node generate-html-bundle.mjs",
    "dev": "npm run build:html && node esbuild.config.mjs",
    "build": "npm run build:html && tsc --noEmit --skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "builtin-modules": "^3.3.0",
    "esbuild": "0.21.4",
    "obsidian": "latest",
    "tslib": "2.6.0",
    "typescript": "5.0.4"
  }
}
